<%- include('partials/header') -%>
<div class="container">
  <div class="row mt-5">
      <div class="col-6">
          <div class="mt-5">
              <h1><%= user.companyName %></h1>
              <p><%= user.description %></p>
              <p><%= user.headquarters %></p>
              <p><%= user.websiteURL %></p>
              <p><%= user.contactInfo %></p>
              <p><%= user.following %></p>
          </div>
      </div>
    </form>
      <div class="col-6">
        <ul class="row list-unstyled">
        <% for(var i=0; i<updates.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/update/<%= updates[i]._id%>">
              <h2><%= updates[i].description%></h2>
              <img class="img-fluid" src="<%= updates[i].media%>">
              <h3><%= updates[i].links%></h3>
            </a>
          </li>
        <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
          <a class="btn btn-primary" href="/feed">My Feed</a>
        </div>  
        <div class="row justify-content-center mt-5">
          <a class="btn btn-primary" href="/discover">Discover</a>
        </div> 
      </div>  
    </div>
  </div>
</div>
<%- include('partials/footer') -%>